apiVersion: v1
kind: Service
metadata:
  name: notes-web
spec:
  selector:
    app: notes
    component: web        # 必須跟 Deployment 的 template labels 完全一致
  ports:
    - port: 80
      targetPort: 80
